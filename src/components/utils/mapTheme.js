// Which biome the current map uses ('grass' | 'desert' | 'snow' | 'volcanic').
// Set once when a round starts (see GamePage.jsx init), read by block.js and
// enemy.js so each map's tiles and enemies match its theme.
export const mapTheme = { value: 'grass' };
